# API документация SferaTC Bot

## Обзор API

SferaTC Bot предоставляет REST API через FastAPI для внешней интеграции и мини-приложение.

## Базовая информация

- **Base URL:** `https://your-domain.com`
- **Content-Type:** `application/json`
- **Аутентификация:** Через Telegram Web App данные

## Эндпоинты

### 1. Мини-приложение

#### GET /
Возвращает HTML страницу мини-приложения.

**Ответ:**
- `200 OK` - HTML страница

**Заголовки:**
```
Cache-Control: no-store, no-cache, must-revalidate
Pragma: no-cache
Expires: 0
```

### 2. Telegram Webhooks

#### POST /{WEBHOOK_PATH}
Обрабатывает обновления от Telegram.

**Заголовки запроса:**
```
Content-Type: application/json
X-Telegram-Secret-Token: your_secret_token
```

**Тело запроса:**
```json
{
  "update_id": 123456789,
  "message": {
    "message_id": 123,
    "from": {
      "id": 123456789,
      "is_bot": false,
      "first_name": "User",
      "username": "username"
    },
    "chat": {
      "id": 123456789,
      "type": "private"
    },
    "date": 160945920,
    "text": "/start"
  }
}
```

**Ответы:**
- `200 OK` - обновление обработано успешно
- `403 Forbidden` - неверный секретный токен
- `400 Bad Request` - неверный формат данных
- `500 Internal Server Error` - ошибка сервера

## Команды бота

### Пользовательские команды

#### /start
Начать работу с ботом

**Описание:** Инициализирует пользователя и показывает главное меню

#### /help
Показать справку

**Описание:** Отображает информацию о доступных командах и функциях

#### /training
Пройти бесплатное обучение

**Описание:** Предоставляет доступ к образовательным материалам

#### /psychologist
Обратиться к ИИ-психологу

**Описание:** Начинает сессию с ИИ-психологом для психологической поддержки

#### /tools
Полезные инструменты

**Описание:** Показывает меню с инструментами для трейдинга

#### /chatgpt
Начать чат с ChatGPT

**Описание:** Инициализирует сессию с ChatGPT для интеллектуальных ответов

#### /support
Получить поддержку

**Описание:** Начинает сессию поддержки с возможностью эскалации администратору

### Административные команды

#### /admin
Панель администратора

**Описание:** Открывает административную панель (только для администраторов)

#### /approve [user_id]
Одобрить пользователя

**Описание:** Одобряет пользователя для доступа к функциям бота

#### /stats
Показать статистику

**Описание:** Отображает статистику использования бота

#### /status
Статус системы

**Описание:** Показывает текущее состояние системы

#### /reset_user [user_id]
Сбросить состояние пользователя

**Описание:** Сбрасывает состояние пользователя в системе

## Состояния пользователей

### UserState Enum

| Состояние | Описание |
|-----------|----------|
| `DEFAULT` | Состояние по умолчанию |
| `CHATGPT_ACTIVE` | Активная сессия ChatGPT |
| `CHATGPT_STREAMING` | Стриминг ответа ChatGPT |
| `SUPPORT_LLM_ACTIVE` | Активная сессия поддержки |
| `AWAITING_SUPPORT_MESSAGE` | Ожидание сообщения поддержки |
| `AWAITING_VERIFICATION_ID` | Ожидание ID для верификации |

### AdminState Enum

| Состояние | Описание |
|-----------|----------|
| `DEFAULT` | Состояние по умолчанию |
| `BROADCAST_AWAITING_MESSAGE` | Ожидание сообщения для рассылки |
| `BROADCAST_AWAITING_CONFIRMATION` | Ожидание подтверждения рассылки |
| `USERS_AWAITING_ID` | Ожидание ID пользователя |
| `USERS_AWAITING_DM` | Ожидание личного сообщения |

## Инструменты трейдинга

### Дисконтные брокеры

#### Tiger.com
- **Описание:** Единая платформа для торговли на нескольких биржах
- **Скидки:** Максимальные скидки на комиссии
- **Функции:** Автоматический дневник сделок, управление рисками

#### Vataga Crypto
- **Описание:** Торговля на всех крупных биржах через одну платформу
- **Скидки:** Специальные условия для пользователей бота
- **Функции:** Продвинутые графики, мультиаккаунт

#### Whitelist
- **Описание:** Онлайн-офис для скальперов
- **Скидки:** Доступ к профессиональным инструментам
- **Функции:** Терминал Scalpee, сообщество трейдеров

## Интеграция с внешними сервисами

### OpenRouter API
- **URL:** `https://openrouter.ai/api/v1`
- **Модели:** DeepSeek R1, GLM-4.5-Air (бесплатные)
- **Функции:** ChatGPT интеграция, стриминг ответов

### Telegram API
- **Webhooks:** Настраиваемые эндпоинты для получения обновлений
- **Bot API:** Отправка сообщений, управление клавиатурами
- **Web Apps:** Интеграция с мини-приложением

## Ошибки и обработка

### Коды ошибок

| Код | Описание | Решение |
|-----|----------|---------|
| `400` | Неверный формат данных | Проверьте формат запроса |
| `403` | Доступ запрещен | Проверьте секретный токен |
| `429` | Слишком много запросов | Включите rate limiting |
| `500` | Внутренняя ошибка сервера | Проверьте логи сервера |

### Глобальная обработка ошибок

Бот включает глобальную обработку ошибок, которая:
- Логирует все неперехваченные исключения
- Уведомляет администратора о критических ошибках
- Предоставляет детальную информацию для отладки

## Примеры использования

### Отправка сообщения администратору об ошибке

```python
from services.notifier import Notifier

async def report_error(bot, error: Exception):
    notifier = Notifier(bot)
    await notifier.send_admin_notification(
        f"🚨 Критическая ошибка: {str(error)}",
        parse_mode="HTML"
    )
```

### Работа с состояниями пользователя

```python
from services.state_manager import StateManager

async def set_user_state(update: Update, context: ContextTypes.DEFAULT_TYPE):
    state_manager = StateManager(context)
    state_manager.set_user_state(UserState.CHATGPT_ACTIVE)
```

### Отправка уведомления пользователю

```python
from telegram import InlineKeyboardMarkup, InlineKeyboardButton

async def send_notification(bot, user_id: int, text: str):
    keyboard = InlineKeyboardMarkup([
        [InlineKeyboardButton("Поддержка", callback_data="support")]
    ])
    
    await bot.send_message(
        chat_id=user_id,
        text=text,
        reply_markup=keyboard,
        parse_mode="HTML"
    )