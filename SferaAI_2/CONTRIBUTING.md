# Руководство по участию в разработке Sfera AI_2

Спасибо за интерес к развитию Sfera AI_2! Этот документ поможет вам внести свой вклад в развитие проекта.

## 🎯 Как помочь проекту

Мы приветствуем вклад в следующих областях:
- 🐛 **Исправление багов** и улучшение стабильности
- ✨ **Новые функции** и возможности
- 📝 **Улучшение документации** и примеров
- 🎨 **UI/UX улучшения** и дизайн
- 🧪 **Тестирование** и quality assurance
- 🔧 **DevOps** и инфраструктура

## 🚀 Быстрый старт для разработчиков

### Предварительные требования
- Node.js 18+ и pnpm
- Python 3.11+
- Git
- Базовые знания: TypeScript, Python, React, LiveKit

### 1. Настройка среды разработки
```bash
# Клонирование репозитория
git clone https://github.com/your-repo/SferaAI_2.git
cd SferaAI_2

# Настройка backend
pip install -r requirements.txt

# Настройка frontend
cd frontend
pnpm install
```

### 2. Настройка переменных окружения
```bash
# Скопируйте и настройте .env файлы
cp .env.example .env
cp frontend/.env.example frontend/.env.local

# Заполните необходимые API ключи для разработки
```

### 3. Запуск в режиме разработки
```bash
# Терминал 1: Backend
python agent.py

# Терминал 2: Frontend
cd frontend && pnpm dev
```

## 📋 Процесс разработки

### 1. Создание ветки
```bash
# Создайте новую ветку для вашей функции
git checkout -b feature/your-feature-name
# или для исправления бага:
git checkout -b bugfix/issue-description
```

### 2. Разработка
```bash
# Разработайте функцию, следуя нашим стандартам кодирования
# Обязательно добавьте тесты для нового кода
```

### 3. Тестирование
```bash
# Запустите все тесты
python test_*.py
cd frontend && pnpm test

# Проверьте качество кода
python -m black .
python -m isort .
cd frontend && pnpm lint
```

### 4. Отправка Pull Request
```bash
# Зафиксируйте изменения
git add .
git commit -m "feat: add new feature description"

# Отправьте ветку
git push origin feature/your-feature-name
```

## 📐 Стандарты кодирования

### Python (Backend)
- **PEP 8** для стилистики кода
- **Black** для форматирования (88 символов в строке)
- **isort** для сортировки импортов
- **mypy** для проверки типов
- **pytest** для тестирования

```bash
# Форматирование Python кода
black .
isort .
mypy .

# Запуск тестов
pytest
```

### TypeScript/JavaScript (Frontend)
- **ESLint + Prettier** для форматирования
- **TypeScript строгий режим**
- **React Hooks** и современные паттерны
- **Test Library** для тестирования компонентов

```bash
# Форматирование и проверка
cd frontend
pnpm lint
pnpm format

# Запуск тестов
pnpm test
```

### Документация
- **Комментарии к коду** на английском языке
- **Docstrings** для всех публичных функций
- **Type hints** для Python функций
- **TypeScript типы** для всех компонентов

## 🧪 Тестирование

### Backend тесты
- **pytest** для unit тестов
- **coverage** для анализа покрытия
- **Интеграционные тесты** для проверки работы с API

```bash
# Запуск всех тестов
pytest --cov=qdrant_memory_client --cov-report=html

# Только определенные тесты
pytest test_integration.py -v
```

### Frontend тесты
- **Jest + Testing Library** для компонентов
- **Playwright** для e2e тестирования
- **Vitest** для быстрых unit тестов

```bash
cd frontend

# Unit тесты
pnpm test

# E2E тесты
pnpm test:e2e

# Покрытие тестами
pnpm test:coverage
```

## 🏗️ Архитектурные принципы

### 1. Модульность
- **Разделение ответственности** между компонентами
- **Минимальные зависимости** между модулями
- **Четкие интерфейсы** между частями системы

### 2. Производительность
- **Async/await** для всех I/O операций
- **Эффективная векторизация** в системе памяти
- **Ленивая загрузка** компонентов во frontend

### 3. Безопасность
- **Валидация всех входных данных**
- **Sanitization** пользовательского контента
- **Безопасное хранение** API ключей

## 📝 Примеры разработки

### Добавление нового инструмента (tool)
```python
# tools.py
from typing import Dict, Any
import logging

@function_tool()
async def get_currency_rates(base_currency: str = "USD") -> str:
    """
    Получение актуальных курсов валют
    
    Args:
        base_currency: Базовая валюта (по умолчанию USD)
    
    Returns:
        JSON строка с курсами валют
    """
    try:
        # Реализация получения курсов
        rates = await fetch_currency_rates(base_currency)
        return json.dumps(rates)
    except Exception as e:
        logging.error(f"Failed to get currency rates: {e}")
        return f"Error: Unable to fetch currency rates"
```

### Добавление нового React компонента
```tsx
// components/common/StatusIndicator.tsx
import React from 'react';
import { cn } from '@/lib/utils';

interface StatusIndicatorProps {
  status: 'online' | 'offline' | 'connecting';
  className?: string;
}

export const StatusIndicator: React.FC<StatusIndicatorProps> = ({
  status,
  className
}) => {
  const statusColors = {
    online: 'bg-green-500',
    offline: 'bg-red-500', 
    connecting: 'bg-yellow-500 animate-pulse'
  };

  return (
    <div
      className={cn(
        'w-3 h-3 rounded-full',
        statusColors[status],
        className
      )}
    />
  );
};
```

## 🐛 Сообщение об ошибках

При сообщении об ошибке, пожалуйста, включите:

### Описание проблемы
- Четкое описание что пошло не так
- Ожидаемое vs фактическое поведение

### Шаги для воспроизведения
1. Откройте...
2. Нажмите на...
3. Прокрутите до...
4. Увидите ошибку

### Информация об окружении
```
OS: [e.g. Windows 11]
Browser: [e.g. Chrome 118]
Node.js: [e.g. 18.17.0]
Python: [e.g. 3.11.5]
```

### Логи ошибок
```
Backend logs:
[ERROR] 2025-11-17T07:48:00Z - Agent initialization failed: ...

Frontend errors:
TypeError: Cannot read properties of undefined
```

## 📊 Отслеживание разработки

### Issue Templates
Мы используем GitHub Issues с шаблонами для:
- 🐛 Bug Reports
- ✨ Feature Requests  
- 📝 Documentation Improvements
- 🔧 Technical Tasks

### Pull Request процесс
1. **Review**: Минимум 1 approving review
2. **CI/CD**: Все тесты должны пройти
3. **Quality**: Проверка linting и formatting
4. **Documentation**: Обновление документации при необходимости

## 🎯 Roadmap проекта

### Текущие приоритеты
- 🖼️ **Computer Vision** возможности
- 🖱️ **Computer Use** функциональность
- 📱 **Mobile приложения**

### Кандидаты для первых контрибуций
- Улучшение документации
- Добавление тестов
- UI/UX улучшения
- Исправление мелких багов

## 🤝 Сообщество

### Каналы связи
- **GitHub Discussions**: Общие вопросы и идеи
- **Discord**: [ссылка на Discord] для разработчиков
- **Email**: developers@sfera-ai.com

### Code of Conduct
Мы придерживаемся Contributor Covenant Code of Conduct. Пожалуйста, прочитайте наш [Code of Conduct](CODE_OF_CONDUCT.md) перед участием.

## 📜 Лицензия

Участвуя в проекте, вы соглашаетесь с тем, что ваш вклад будет лицензирован под той же лицензией, что и основной проект (MIT License).

## 🙏 Благодарности

Спасибо всем контрибьютерам Sfera AI_2! Ваш вклад делает проект лучше.

**Особая благодарность**:
- Первым контрибьютерам
- Авторам pull requests
- Тестировщикам
- Авторам баг-репортов

---

**Готовы начать?** Выберите Issue из [списка задач](https://github.com/your-repo/SferaAI_2/issues) и приступайте к разработке! 🎉